/**
 * @param {object} target 
 */
function flat(target) {
  const res = {}
  /**
   * 
   * @param {*} item 需要被递归处理的子元素
   * @param {Array<string>} 前置路径数组
   */
  const process = (item, arr) => {

  }
}